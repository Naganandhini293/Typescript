let example: [string, number] = ["aaa", 3];

example.push(679); //--> this is allowed

// let example1: [string, number] = [9, "abcd"]; // --> order of array should be string, number not number, string

export {};
