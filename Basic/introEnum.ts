const enum SeatChoice { // -> const keyword to avoid unneccasary JS code
    AISLE = "aisle", // ->0
    MIDDLE = 0, // ->1
    WINDOW, // ->2
    FOURTH, // ->3
}

let choice = SeatChoice.AISLE;

export {};
